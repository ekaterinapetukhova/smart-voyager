export enum RouterEnum {
  Auth = "/auth",
  Trips = "/trips",
  Registration = "/registration",
  Login = "/login",
  Verification = "/verify-email",
  TripMates = "/trip-mates",
  Chats = "/chats",
  NotFound = "/not-found",
}
